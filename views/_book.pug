mixin book(book)
  li.book(data-rating=book.rating data-isbn=book.isbn)
    a(href=book.link)= book.title_without_series
    |
    |
    span.by by
    |
    |
    each author in book.authors
      span.author= author
